# 可选：若不用 kubectl create secret，可复制为 secret.yaml 后 apply（勿提交真实密码）
# 推荐做法见《通用部署操作》4.3 步骤 1 的 kubectl 命令
apiVersion: v1
kind: Secret
metadata:
  name: redis-secret
  namespace: mall-middleware
type: Opaque
stringData:
  requirepass: "请改成强密码"
