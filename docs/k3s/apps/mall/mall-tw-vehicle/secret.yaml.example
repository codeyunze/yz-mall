# 复制为 secret.yaml 后填写，secret.yaml 不要提交 Git
# 若 mall 命名空间已有 nacos-secret，可跳过本文件
apiVersion: v1
kind: Secret
metadata:
  name: nacos-secret
  namespace: mall-business
type: Opaque
stringData:
  password: "Nacos控制台密码"
