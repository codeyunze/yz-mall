spring:
  cloud:
    compatibility-verifier:
      enabled: false
    nacos:
      server-addr: 127.0.0.1:8848
      username: nacos
      password: ${NACOS_PASSWORD}
      config:
        namespace: mall
        import-check:
          enabled: false
        extension-configs:
          - data-id: mall-gateway.yaml
            refresh: true
      discovery:
        namespace: mall