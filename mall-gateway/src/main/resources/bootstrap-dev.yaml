spring:
  cloud:
    nacos:
      server-addr: yunze.com:8848
      config:
        namespace: mall
        extension-configs:
          - data-id: mall-gateway.yaml
            refresh: true
      discovery:
        namespace: mall
      username: nacos
      password: ${NACOS_PASSWORD}