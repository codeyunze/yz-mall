<?xml version="1.0" encoding="UTF-8"?>
<configuration debug="false">
    <!--从 spring.application.name 读取服务名，日志目录与 Logstash 标签自动跟随，新模块只需配置服务名即可-->
    <springProperty scope="context" name="APP_NAME" source="spring.application.name" defaultValue="application"/>
    <!--设置存储路径变量-->
    <property name="LOG_HOME" value="./logs/${APP_NAME}/"/>

    <!-- 每次打日志刷新 SkyWalking trace_id/span_id，避免一直停在入口 Span 的 0 -->
    <turboFilter class="com.yz.mall.base.trace.SkyWalkingMdcTurboFilter"/>

    <!--控制台输出appender-->
    <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
        <!--设置输出格式-->
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <!--格式化输出：%d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度%msg：日志消息，%n是换行符；%highlight/%green/%magenta/%cyan 为控制台着色-->
            <pattern>%green(%d{yyyy-MM-dd HH:mm:ss.SSS}) %magenta([%thread]) %highlight(%-5level) [%X{trace_id}] [%X{span_id}] %cyan(%logger{50}) - %msg%n</pattern>
            <!--设置编码-->
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!--文件输出,时间窗口滚动-->
    <appender name="timeFileOutput" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <!--日志名,指定最新的文件名，其他文件名使用FileNamePattern -->
        <File>${LOG_HOME}/out.log</File>
        <!--文件滚动模式-->
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <!--日志文件输出的文件名,可设置文件类型为gz,开启文件压缩-->
            <FileNamePattern>${LOG_HOME}/info.%d{yyyy-MM-dd}.%i.log.gz</FileNamePattern>
            <!--日志文件保留天数-->
            <MaxHistory>30</MaxHistory>
            <!--按大小分割同一天的-->
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
        </rollingPolicy>

        <!--输出格式-->
        <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
            <!--格式化输出：%d表示日期，%thread表示线程名，%-5level：级别从左显示5个字符宽度%msg：日志消息，%n是换行符-->
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level [%X{trace_id}] [%X{span_id}] %logger{70} - %msg%n</pattern>
            <!--设置编码-->
            <charset>UTF-8</charset>
        </encoder>

    </appender>

    <appender name="logstash" class="net.logstash.logback.appender.LogstashTcpSocketAppender">
        <destination>yunze.com:4560</destination>
        <encoder class="net.logstash.logback.encoder.LogstashEncoder">
            <customFields>{"platform": "mall", "app_name": "${APP_NAME}"}</customFields>
            <!-- MDC 由 TraceGatewayFilter 用 WebFluxSkyWalkingTraceContext 刷新，与下游 SW8 同源 -->
            <includeMdcKeyName>trace_id</includeMdcKeyName>
            <includeMdcKeyName>span_id</includeMdcKeyName>
            <includeMdcKeyName>segment_id</includeMdcKeyName>
            <includeMdcKeyName>client_ip</includeMdcKeyName>
        </encoder>
    </appender>

    <!-- MyBatis Mapper SQL 日志包：由 app.logging.mapper-package 指定，默认 com.yz.mall（新模块只需改 application.yaml） -->
    <springProperty scope="context" name="MAPPER_PACKAGE" source="app.logging.mapper-package" defaultValue="com.yz.mall"/>
    <logger name="${MAPPER_PACKAGE}" level="DEBUG"/>
    <!-- 公共组件 Mapper（各服务通用，无对应包时无影响） -->
    <logger name="io.github.codeyunze.mapper" level="DEBUG"/>

    <!--指定基础的日志输出级别-->
    <root level="INFO">
        <!--appender将会添加到这个loger-->
        <appender-ref ref="console"/>
        <appender-ref ref="timeFileOutput"/>
        <appender-ref ref="logstash"/>
    </root>
</configuration>
